shader_type canvas_item;


uniform sampler2D simulation_texture;


void vertex() {
	// Called for every vertex the material is visible on.

	// vec4 COLOR
	// vec2 VERTEX
	// vec2 UV
	// float POINT_SIZE
	// maybe should be a compute shader
}
